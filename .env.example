# The app reads GitHub credentials from the in-app Settings screen, not from
# this file — a client-side bundle cannot hold a secret, so anything put here
# with a VITE_ prefix would be compiled into dist/ and readable by anyone.
#
# This file exists only to document the shape of local config. Copy it to .env
# (which is gitignored) if you add build-time settings later.
